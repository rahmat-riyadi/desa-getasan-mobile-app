import 'package:flutter/material.dart';

class MailManagement extends StatelessWidget {
  const MailManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child:  Container(
        decoration: const BoxDecoration(),
        child: const Center(child: Text('persuratan'),),
      )
    );
  }
}